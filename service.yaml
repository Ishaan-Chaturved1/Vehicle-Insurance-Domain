apiVersion: v1
kind: Service
metadata:
  name: vehicle-insurance-service
spec:
  type: LoadBalancer
  selector:
    app: vehicle-insurance
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000